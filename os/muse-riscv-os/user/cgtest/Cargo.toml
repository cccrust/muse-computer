[package]
name = "cgtest"
version = "0.1.0"
edition = "2021"

[dependencies]
user-lib = { path = "../../user-lib" }

[profile.release]
opt-level = "s"
lto = true
codegen-units = 1
panic = "abort"

[profile.dev]
opt-level = "s"
panic = "abort"
